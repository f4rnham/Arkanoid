unit MainMenu;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils, FileUtil, Forms, Controls, Graphics, Dialogs, StdCtrls, unit1;

type

  { Tmenu }

  Tmenu = class(TForm)
    start: TButton;
    procedure startClick(Sender: TObject);
  private
    { private declarations }
  public
    { public declarations }
  end; 

var
  menu: Tmenu;

implementation

{$R *.lfm}

{ Tmenu }

procedure Tmenu.startClick(Sender: TObject);
begin
   Tmenu.Hide;
   TMAIN.Show;
end;

end.

